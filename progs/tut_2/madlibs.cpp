#include <iostream>
#include <cmath>
#include <string>
using namespace std;

int main() {

    string color, pluralNoun, celebrity;

    // Don't use 'cin' for strings, use getline();

    cout << "Enter a color: ";
    getline(cin, color);

    // I was too lazy to create this part, just used this to learn getline()
    return 0;
}